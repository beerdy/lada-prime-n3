= form_for @special do |f|
  - if @special.errors.any?
    #error_explanation
      %h2= "#{pluralize(@special.errors.count, "error")} prohibited this special from being saved:"
      %ul
        - @special.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :slave
    = f.text_area :slave
  .field
    = f.label :link
    = f.text_field :link
  .field
    = f.label :url
    = f.text_field :url
  .field
    = f.label :sort
    = f.number_field :sort
  .field
    = f.label :show
    = f.check_box :show
  .actions
    = f.submit 'Save'
